import { HoverMousedDirective } from './hover-moused.directive';

describe('HoverMousedDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverMousedDirective();
    expect(directive).toBeTruthy();
  });
});
