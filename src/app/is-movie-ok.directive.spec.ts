import { IsMovieOkDirective } from './is-movie-ok.directive';

describe('IsMovieOkDirective', () => {
  it('should create an instance', () => {
    const directive = new IsMovieOkDirective();
    expect(directive).toBeTruthy();
  });
});
